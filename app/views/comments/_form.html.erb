<%#= form_with(model: comment, local: true) do |form| %>
  <%# if comment.errors.any? %>
<!--    <div id="error_explanation">-->
<!--      <h2><%#= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>-->

<!--      <ul>-->
        <%# comment.errors.full_messages.each do |message| %>
<!--          <li><%#= message %></li>-->
        <%# end %>
<!--      </ul>-->
<!--    </div>-->
  <%# end %>

<!--  <div class="actions">-->
    <%#= form.submit %>
<!--  </div>-->
<%# end %>




<!--<p class="text-sm text-grey-dark my-2">Post as <%#= current_user.username %></p>-->

<%#= form_for([@eventpost, @comment], remote: true) do |f| %>
<!--  <div class="relative">-->
    <%#= f.text_area :content, class: "input h-32 border border-grey-light resize-none", placeholder: "Post content" %>
<!--    <div class="actions">-->
      <%#= f.submit %>
<!--    </div>-->
<!--  </div>-->
<%# end %>




<p class="text-sm text-grey-dark my-2">comment as <%= current_user.username %></p>

<%= form_for([@event, @eventpost, @comment], remote: true) do |f| %>
  <div class="relative">
    <%= f.hidden_field :content, class: "input h-32 border border-grey-light resize-none", placeholder: "Add any notes or replies here" %>

<!--    <trix-editor input="comment_reply" id="comment_reply_trix" class="h-32 p-4"></trix-editor>-->

    <%= f.submit class: "btn btn-small btn-secondary block w-full text-center mt-2 lg: mt-0 lg:text-left lg:absolute lg:pin-b lg:pin-r lg:mb-2 lg:mr-2 lg:w-auto lg:inline-flex" %>
  </div>
<% end %>